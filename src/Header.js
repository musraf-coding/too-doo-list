import React from 'react'

const Header = (props) => {
    // const headerStyle ={backgroundColor:"blue"}
  return (
    <header>
        <h1>{props.tittle}</h1>
    </header>
  )
}


export default Header